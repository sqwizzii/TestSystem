@model List<TestSystem.Entities.Test>

<h2>Список тестів</h2>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Назва</th>
            <th>Дії</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var test in Model)
        {
            <tr>
                <td>@test.Id</td>
                <td>@test.Title</td>
                <td>
                    <a class="btn btn-sm btn-primary" href="/teacher/view-test/@test.Id">Переглянути</a>
                    <a class="btn btn-sm btn-success" href="/teacher/add-question/@test.Id">Додати запитання</a>
                </td>
            </tr>
        }
    </tbody>
</table>
