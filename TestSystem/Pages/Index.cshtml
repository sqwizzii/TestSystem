@page
@model IndexModel
@{
    ViewData["Title"] = "Список тестів";
}

<h2>Список тестів</h2>

@foreach (var test in Model.Tests)
{
    <div class="card mb-2 p-3">
        <h5>@test.Title</h5>
        <a class="btn btn-primary" asp-page="/Take" asp-route-id="@test.Id">Пройти</a>
    </div>
}